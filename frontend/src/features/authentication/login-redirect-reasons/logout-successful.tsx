import { Alert } from 'evergreen-ui';
import React, { StatelessComponent } from 'react';

export const LogoutSuccessful: StatelessComponent = () => (
  <Alert intent="none" title="You have been logged out successfully." />
);
